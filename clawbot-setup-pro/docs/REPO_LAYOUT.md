# Repo Layout

- `mobile/` Flutter app (iOS + Android)
- `backend/` FastAPI backend (auth, entitlements, devices, orchestration)
- `desktop-helper/` Go desktop helper (Windows + macOS)
- `docs/` Product + architecture docs
